<?php

$str = "C'est trop dur";
$char = "r";

function occurrences($str, $char) {
    $tableau =[];
    $index = 0;
    for (isset($str($index))) {
        $tableau[$str] = $str[$index];
        $index++;

        if ($index == $char) {
            return $nbr;
        }
    }
}